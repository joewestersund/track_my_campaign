<h1>Listing heal_city_designations</h1>

<table>
  <thead>
    <tr>
      <th>Database instance</th>
      <th>Name</th>
      <th>Order in list</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @heal_city_designations.each do |heal_city_designation| %>
      <tr>
        <td><%= heal_city_designation.database_instance_id %></td>
        <td><%= heal_city_designation.name %></td>
        <td><%= heal_city_designation.order_in_list %></td>
        <td><%= link_to 'Show', heal_city_designation %></td>
        <td><%= link_to 'Edit', edit_heal_city_designation_path(heal_city_designation) %></td>
        <td><%= link_to 'Destroy', heal_city_designation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Heal city designation', new_heal_city_designation_path %>
