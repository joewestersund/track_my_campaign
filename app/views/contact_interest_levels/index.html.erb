<h1>Listing contact_interest_levels</h1>

<table>
  <thead>
    <tr>
      <th>Database instance</th>
      <th>Name</th>
      <th>Order in list</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @contact_interest_levels.each do |contact_interest_level| %>
      <tr>
        <td><%= contact_interest_level.database_instance_id %></td>
        <td><%= contact_interest_level.name %></td>
        <td><%= contact_interest_level.order_in_list %></td>
        <td><%= link_to 'Show', contact_interest_level %></td>
        <td><%= link_to 'Edit', edit_contact_interest_level_path(contact_interest_level) %></td>
        <td><%= link_to 'Destroy', contact_interest_level, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contact interest level', new_contact_interest_level_path %>
