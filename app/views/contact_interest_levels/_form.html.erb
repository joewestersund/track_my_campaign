<%= form_for(@contact_interest_level) do |f| %>
  <% if @contact_interest_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_interest_level.errors.count, "error") %> prohibited this contact_interest_level from being saved:</h2>

      <ul>
      <% @contact_interest_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :database_instance_id %><br>
    <%= f.number_field :database_instance_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :order_in_list %><br>
    <%= f.number_field :order_in_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
