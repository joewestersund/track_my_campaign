<h1><%= (@user == current_user) ? "Edit Your Profile" : "Edit User" %></h1>

<%= render 'form', locals: { new_user: false, is_current_user: @user == current_user } %>

<%= link_to 'Back to Index', users_path if current_user.admin? %>
