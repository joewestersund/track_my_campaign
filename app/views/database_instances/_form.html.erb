<%= form_for(@database_instance, as: :database_instance, url: (locals["new"] ? new_database_instance_path : database_instance_path(@database_instance))) do |f| %>
  <% if @database_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@database_instance.errors.count, "error") %> prohibited this database_instance from being saved:</h2>

      <ul>
      <% @database_instance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label "Database Type" %>
    <%= f.select :type, @database_types %>
  </div>
  <div class="field">
    <%= f.label :instance_name %>
    <%= f.text_field :instance_name %>
  </div>
  <div class="actions">
    <%= f.submit locals[:new] ? "Create" : "Update", class: "btn btn-large btn-primary"%>
  </div>
<% end %>
