<%= form_for(@resolution) do |f| %>
  <% if @resolution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resolution.errors.count, "error") %> prohibited this resolution from being saved:</h2>

      <ul>
      <% @resolution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date, {class: 'datepicker'} %>
  </div>
  <div class="field">
    <%= f.label :city %>
    <%= f.collection_select :city_id, @cities, :id, :name_type_state, {include_blank: true}, {class: 'multiselect', multiple: false}  %>
  </div>
  <div class="field">
    <%= f.label :policies_adopted_by_resolution %>
    <%= f.collection_select :policy_ids, @policies, :id, :name, {}, {class: 'multiselect', multiple: true} %>
  </div>
  <div class="field">
    <%= f.label :prior_to_joining_campaign %>
    <%= f.check_box :prior_to_joining_campaign %>
  </div>
  <div class="field">
    <%= f.label :notes %>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
