<%= form_for(@city_designation) do |f| %>
  <% if @city_designation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@city_designation.errors.count, "error") %> prohibited this city_designation from being saved:</h2>

      <ul>
      <% @city_designation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :order_in_list %>
    <%= f.number_field :order_in_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
