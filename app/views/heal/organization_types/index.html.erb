<h1>Listing Organization Types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Order in list</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @organization_types.each do |organization_type| %>
      <tr>
        <td><%= organization_type.name %></td>
        <td><%= organization_type.order_in_list %></td>
        <td><%= link_to 'Edit', edit_heal_organization_type_path(organization_type) %></td>
        <td><%= link_to 'Destroy', organization_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Organization Type', new_heal_organization_type_path %>
