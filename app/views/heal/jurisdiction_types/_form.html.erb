<%= form_for(@jurisdiction_type) do |f| %>
  <% if @jurisdiction_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jurisdiction_type.errors.count, "error") %> prohibited this jurisdiction_type from being saved:</h2>

      <ul>
      <% @jurisdiction_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :order_in_list %><br>
    <%= f.number_field :order_in_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
