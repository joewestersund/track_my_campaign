<%= form_for(@position_type) do |f| %>
  <% if @position_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@position_type.errors.count, "error") %> prohibited this position_type from being saved:</h2>

      <ul>
      <% @position_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :order_in_list %>
    <%= f.number_field :order_in_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
